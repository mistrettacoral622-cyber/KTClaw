请根据 03-CONTEXT.md 中的设计决策，重新调整和美化「团队总览」页面的 UI 布局。目前的界面视觉效果较差，且没有严格遵循文档中的布局规范。请重点进行以下重构：

1. 页面整体布局结构优化

左侧主内容区：背景应保持统一的浅灰色调，确保与卡片形成对比。

右侧 Agent 面板 (D-08, D-09)：面板需要有清晰的边界或阴影与主内容区分开。确保它具备折叠/展开功能，Agent 卡片样式需要更精致（例如增加 hover 态、边框变色等）。

2. 彻底重构空状态引导 (D-19)

当前「还没有团队」的提示和「创建新团队」的虚线框并排显示，显得十分怪异。

当团队数量为 0 时，页面中心只应该显示一个精美的大型引导卡片。

卡片内容需包含：一个更有设计感的空状态图标、大标题「还没有团队」、描述文字「从右侧拖拽 Agent 到左侧创建区来创建第一个团队」，并预留动画演示的位置。

3. 规范「创建新团队」区域 (D-11, D-12)

这个区域不应该像一个普通的团队卡片一样混排。它应该是一个固定在左侧或顶部的专属拖拽目标区（Dropzone）。

请使用更柔和的虚线边框、浅色背景（如底色带一点点蓝或主色调的极浅色），使其看起来明显是一个「可放入」的区域。

视觉上提前规划好 Leader 区和成员区的分隔线或占位符，即使目前还没有完全实现拖拽逻辑，UI 上也需要体现出「Leader」和「Members」的位置差异。

4. 提升整体 UI 质感与排版

使用 Tailwind CSS 优化间距（padding/margin），增加呼吸感（参考 D-02）。

文字排版需要层级分明，加粗大标题，淡化次要说明文字（使用 text-muted-foreground 等）。

去除所有生硬的线条，多使用柔和的圆角（rounded-xl或rounded-2xl）和轻微的阴影（shadow-sm）。

请优先完成静态 UI 的极致打磨，确保它符合一个现代化、极简风的 SaaS 级产品标准，完成布局后再逐步引入复杂的交互逻辑。